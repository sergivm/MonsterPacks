package com.sergivm.monsterpacks.domain

/**
 * Global configuration constants for the app.
 */
object Config {
    /**
     * Enables developer-specific behaviors for faster testing:
     * - Equal drop rates for all rarities.
     * - 100x resource (Coins/Gems) rewards.
     */
    const val DEV_BOOST = true
}
